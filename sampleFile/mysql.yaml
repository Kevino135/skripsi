server:
    port: ":8080"

database:
    aura: "Phars:d3v3l0@tcp(aurora.travel.id)/q_love?parseTime=true"

api:
    auth: "https://staging-api.travel.id/auth"
    
swagger:
    host: "localhost:8080"
    schemes:
        - http